# 构建日志与状态
logs/
.build-state.json

# 本地备忘（含本机路径 / 敏感信息，不随仓库发布）
fork-sync-notes.md

# 依赖与构建产物
# （dsh-desktop 是独立仓库，由 build.ps1 直接克隆 / 更新管理，其内部文件由
#   dsh-desktop 自己的 .gitignore 管理，父仓库不跟踪整个目录）
dsh-desktop/
node_modules/
dist/
*.log
